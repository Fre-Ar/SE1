describe('API: /api/admin (placeholders)', () => {
  it.todo('PATCH /api/admin/users/:id/role updates user role')
  it.todo('POST /api/admin/ip-bans creates an IP ban')
  it.todo('DELETE /api/admin/ip-bans/:ip removes an IP ban')
})
